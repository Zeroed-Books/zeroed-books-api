services:
    postgres:
        image: postgres:13

        environment:
            POSTGRES_PASSWORD: password

        volumes:
            - 'postgres-data:/var/lib/postgresql/data'

    redis:
        image: redis:6

volumes:
    postgres-data: null
